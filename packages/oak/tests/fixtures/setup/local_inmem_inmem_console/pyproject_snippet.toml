# Paste these into your project's pyproject.toml.
# Merge the dependencies list with whatever you already have.

[project]
dependencies = [
    "oak-workspace",
    "oak-session",
    "oak-session[openhands]",
]
