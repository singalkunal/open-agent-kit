# Generated by `oak setup`. Local-dev services only.
# Skip this file if you already run these services elsewhere.

services:
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: oak
      POSTGRES_PASSWORD: oak
      POSTGRES_DB: oak
    ports: ["5432:5432"]
